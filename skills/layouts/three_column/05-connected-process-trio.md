---
id: connected-process-trio
name: connected process trio
layout_type: three_column
order: 5
description: Three steps linked left-to-right by a connector line, each a node with an icon, a pill label on the line, and a caption — for processes.
---

Three steps linked left-to-right by a thin horizontal connector line. Bold section title
top-left; a small 3-dot motif top-right. At the left end of the line a small filled
block/badge marks the start. Each of the three nodes sits above the line with: a
line-style (stroke, transparent-fill) icon, a filled rounded pill label directly on the
line (border_radius 10-14, centered white caption — alternate the three pill fills
across primary / accent / a deeper tint), and a centered 3-4 line caption block below
it. Nodes evenly spaced; the connector passes through the pill centers. Light field.
Target 16-22 elements.
