---
id: comparison-matrix
name: comparison matrix
layout_type: table
order: 1
description: Table as a comparison grid — accent header, zebra rows, and one highlighted winning column/row so the recommendation pops.
---

Table as a comparison grid: accent-filled header row, zebra row tints for scanability,
and ONE highlighted winning column or row (stronger accent tint + a small
badge/checkmark) so the recommendation pops. Row-label column slightly wider.
